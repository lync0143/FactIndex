package org.example;

public interface FactIndex {
    int getFact(String key); // get the integer fact corresponding to the key
                                    // (name of city, name of state,etc.)
}
